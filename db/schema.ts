/**
 * Drizzle ORM schema — full implementation in Phase 2.
 *
 * Tables defined here:
 *   Phase 2: profiles, agents, agent_notes, agent_status_history,
 *            deals, commission_advances, cms_pages, cms_sections,
 *            cms_media, cms_form_schemas, site_config, audit_logs
 */

// Phase 2 placeholder — exported so imports don't break
export const schema = {} as const
